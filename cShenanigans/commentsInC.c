#include <stdio.h>

int main() {
	
//creating a variable
	int integer = 34;

//printing the created variable in a new lin  e

	printf("integer = %d\n", integer);
	
		return 0;
}
//we can also use comments in debugging our code by adding the 2 forward slashes infront of the code that we want to debug
//we can also use /* at the beginning and end
